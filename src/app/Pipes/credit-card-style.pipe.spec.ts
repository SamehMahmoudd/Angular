import { CreditCardStylePipe } from './credit-card-style.pipe';

describe('CreditCardStylePipe', () => {
  it('create an instance', () => {
    const pipe = new CreditCardStylePipe();
    expect(pipe).toBeTruthy();
  });
});
