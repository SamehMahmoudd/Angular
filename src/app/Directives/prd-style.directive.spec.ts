import { PrdStyleDirective } from './prd-style.directive';

describe('PrdStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new PrdStyleDirective();
    expect(directive).toBeTruthy();
  });
});
